.fs-headline
    | รายชื่อผู้จำหน่ายเวชภัณฑ์
    
.data-table-container
    table.data-table.data-table--has-secondary
        thead
            tr
                th #
                th ชื่อบริษัท/ร้านค้า
                th ติดต่อ
                th ที่อยู่
                th เบอร์โทร
                th
            tr
                td(colspan="6").divider.divider--dark
        tbody
            tr(ng-repeat="s in suppliers").data-table__clickable-row
                td {{$index+1}}
                td {{s.name}}
                td {{s.contact_name}}
                td {{s.address}}
                td {{s.telephone}}
                td
                    lx-dropdown(position="right", from-top)
                        button.btn.btn--l.btn--grey.btn--icon(lx-ripple, lx-dropdown-toggle)
                            i.mdi.mdi-dots-vertical
                                
                        lx-dropdown-menu
                            ul
                                li
                                    a(href="#/edit/{{s.id}}").dropdown-link
                                        i.mdi.mdi-pencil
                                        | &nbsp; Edit
                                li
                                    a(ng-click="doRemove(s.id)").dropdown-link
                                        i.mdi.mdi-close
                                        | &nbsp; Delete

div(flex-container="row", flex-gutter="24")
    div(flex-item="11")
        | &nbsp;
    div(flex-item="1")
        br
        br
        a(href="#/add", lx-ripple).btn.btn--m.btn--red.btn--fab
            i.mdi.mdi-plus
