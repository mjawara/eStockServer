.fs-headline
    | รายชื่อผู้จำหน่ายเวชภัณฑ์
    
.data-table-container
    table.data-table.data-table--has-secondary
        thead
            tr
                th #
                th ชื่อบริษัท/ร้านค้า
                th ติดต่อ
                th ที่อยู่
                th เบอร์โทร
                th
        tbody
            tr(ng-repeat="s in suppliers")
                td {{$index+1}}
                td {{s.name}}
                td {{s.contact_name}}
                td {{s.address}}
                td {{s.telephone}}
                td
                    lx-dropdown(position="right", from-top)
                        button.btn.btn--l.btn--black.btn--icon(lx-ripple, lx-dropdown-toggle)
                            i.mdi.mdi--more-vert
                                
                        lx-dropdown-menu
                            ul
                                li
                                    a(href="#/edit/{{s.id}}").dropdown-link 
                                        i.mdi.mdi--edit
                                        | &nbsp; Edit
                                li
                                    a(ng-click="doRemove(s.id)").dropdown-link
                                        i.mdi.mdi--remove-circle
                                        | &nbsp; Delete

.grid
    .grid__col11
        | &nbsp;
    .grid__col1
        br
        br
        a(href="#/add", lx-ripple).btn.btn--l.btn--green.btn--fab
            i.mdi.mdi--add
